import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#ffcb05',
        dark: '#b38e04',
        light: 'e6b705',
      },
      secondary: {
        main: '#3d7dca',
        dark: '#003a70',
        light: '#518acf',
      },
    },
  });

export default theme;